# Backend Environment Variables
# Copy this file to .env and fill in your values

# Server Configuration
PORT=5000
NODE_ENV=development

# JWT Secret (Change this to a strong random string in production!)
# Generate a secure secret: https://www.random.org/strings/
# Or use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production

# CORS Configuration
# Set to your frontend URL in production
# Example: https://your-frontend.vercel.app
CORS_ORIGIN=http://localhost:3000

# Optional: Cloudinary Configuration for Cloud Photo Storage
# Sign up at https://cloudinary.com/ and get your URL
# Format: cloudinary://api_key:api_secret@cloud_name
# CLOUDINARY_URL=cloudinary://...

# Database Configuration (if upgrading from file-based)
# DATABASE_URL=postgresql://user:password@localhost:5432/devdir
# MONGODB_URI=mongodb+srv://user:password@cluster.mongodb.net/devdir
